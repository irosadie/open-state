{
  "name": "@openstate/worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "go run ./cmd/worker/main.go",
    "build": "go build -o dist/worker ./cmd/worker/main.go",
    "start": "./dist/worker",
    "lint": "go vet ./...",
    "test": "go test ./..."
  }
}
